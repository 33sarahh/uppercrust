{
  "name": "uppercrust",
  "version": "1.0.0",
  "private": true,
  "description": "the upper crust - bread ordering app",
  "scripts": {
    "build": "npm ci --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
